{
  "name": "scripts",
  "version": "1.0.0",
  "description": "CI TODO: Move this to indipendent repo and publish to NPM as a lib",
  "main": "deploy.js",
  "scripts": {
    "start": "ts-node deploy.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Pavol Hejný",
  "license": "ISC",
  "dependencies": {
    "compare-versions": "^3.6.0",
    "glob-promise": "^3.4.0",
    "isomorphic-fetch": "^2.2.1",
    "node-ssh": "^7.0.0",
    "promise-sftp": "^0.11.3",
    "ts-node": "^8.6.2",
    "waitasecond": "^1.0.0"
  }
}
